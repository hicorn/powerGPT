# Core dependencies
torch>=2.0.0
numpy>=1.24.0
tiktoken>=0.5.0

# Training & inference
transformers>=4.35.0  # for tokenizer only (optional, we use tiktoken)
accelerate>=0.24.0
bitsandbytes>=0.41.0  # for 4-bit quantization (optional)


# Logging & monitoring
tensorboard>=2.13.0
wandb>=0.15.0

# Configuration
pyyaml>=6.0

# Data processing
datasets>=2.14.0  # optional, for easy dataset loading

# Export formats (optional)
onnx>=1.14.0
onnxruntime>=1.15.0
# torch2trt - requires manual install: git clone https://github.com/NVIDIA-AI-IOT/torch2trt

# Benchmarking
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Production server (optional)
fastapi>=0.104.0
uvicorn>=0.24.0
sse-starlette>=1.6.0

# Code quality (development)
black>=23.0.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.5.0

# Optional: FlashAttention-2 (requires CUDA 11.6+)
